# CryptoPanic free API key — get one at https://cryptopanic.com/developers/api/
# Without this, fetch_sentiment.py uses the bundled sample headlines cache.
CRYPTOPANIC_API_KEY=

# Optional: Binance API key — NOT required for public historical klines
BINANCE_API_KEY=
BINANCE_SECRET_KEY=
